---

- name: Install Fonts for Development
  become: true
  dnf:
    name: "{{ item }}"
    state: present
  loop:
    - "gnome-font-viewer"
    - "cascadia-code-fonts"
    - "jetbrains-mono-fonts-all"
    - "ibm-plex-fonts"
    - "fira-code-fonts"

